const radio = prompt("Indiquenos el valor de radio: ")

const formula = "4/3 * PI * radio4 / 3 * Math.PI * Math.pow(radio, 3)"

const resultado = 4 / 3 * Math.PI * Math.pow(radio, 3)

alert(`El volumen de la esfera se calcula con la fórmula:  ${formula}, por tanto una esfera
    de radio ${radio} tiene un volumen de ${resultado}`)
